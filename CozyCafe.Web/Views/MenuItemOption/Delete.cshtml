@model CozyCafe.Models.Domain.Admin.MenuItemOption

@{
    ViewData["Title"] = "Видалити опцію";
}

<h1 class="text-3xl font-serif mb-6 text-red-700">Підтвердження видалення</h1>

<div class="mb-4">
    Ви впевнені, що хочете видалити опцію: <strong>@Model.Name</strong>?
</div>

<form asp-action="Delete" method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="bg-red-600 hover:bg-red-700 text-white py-2 px-4 rounded font-semibold">Видалити</button>
    <a asp-action="Index" class="ml-4 text-gray-600 hover:underline">Відмінити</a>
</form>
